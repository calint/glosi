{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "glosi build",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "${workspaceFolder}/make.sh",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}