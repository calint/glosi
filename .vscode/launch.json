{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "glosi: debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/glosi", // Adjust the path if necessary
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "glosi: build",
    }
  ]
}